<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.furtest"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.furtest.MainActivity"
            android:label="@string/app_name" >
            
        </activity>
        <service android:name="com.example.ant_test.service2.MyService"></service> 
        
        <activity android:name="com.example.broadcast.BroadcastTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
        </activity>
        <receiver android:name="com.example.broadcast.TestReceiver"
            android:exported="false">
            
        </receiver>
    </application>

</manifest>
